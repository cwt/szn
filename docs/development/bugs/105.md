---
type: bug
title: "Bug 105 — `key.zig` — Alt modifier lost when parsing ESC+char sequences"
description: "105. key.zig — Alt modifier lost when parsing ESC+char sequences"
severity: "HIGH"
status: "✅ FIXED — `parse()` now sets `.mod.alt = true` when returning a char key from a `seq.len == 2` ESC+char sequence. Test updated and format round-trip verified."
file: "`src/key.zig:207–208`"
sources:
  - src/key.zig
verified: human-reviewed
timestamp: 2026-08-03T00:00:00Z
---

# Bug 105 — `key.zig` — Alt modifier lost when parsing ESC+char sequences

### 105. `key.zig` — Alt modifier lost when parsing ESC+char sequences
**File:** `src/key.zig:207–208`
**Severity:** HIGH
**Status:** ✅ FIXED — `parse()` now sets `.mod.alt = true` when returning a char key from a `seq.len == 2` ESC+char sequence. Test updated and format round-trip verified.
