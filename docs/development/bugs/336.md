---
type: bug
title: "Bug 336 — Duplicated key binding flag parsing loop in cfg.zig"
description: "336. parseBindKey and parseUnbindKey duplicate flag parsing loop handling -T and -n."
severity: "LOW"
status: "OPEN"
file: "src/cfg.zig:264, 311"
timestamp: 2026-08-09T00:00:00Z
---

# Bug 336 — Duplicated key binding flag parsing loop in cfg.zig

### 336. Duplicated key binding flag parsing loop in cfg.zig
**File:** `src/cfg.zig:264, 311`
**Severity:** LOW
**Status:** OPEN

The flag parsing loop handling `-T` and `-n` is duplicated across `parseBindKey` and `parseUnbindKey`.

### Root Cause
Duplicated argument parsing logic across configuration parsing functions.

### Impact
Increased maintenance overhead and potential parsing logic divergence.
