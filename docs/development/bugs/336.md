---
type: bug
title: "Bug 336 — Duplicated key binding flag parsing loop in cfg.zig"
description: "336. parseBindKey and parseUnbindKey duplicate flag parsing loop handling -T and -n."
severity: "LOW"
status: "✅ FIXED"
file: "src/cfg.zig:257-310"
timestamp: 2026-08-09T00:00:00Z
---

# Bug 336 — Duplicated key binding flag parsing loop in cfg.zig

### 336. Duplicated key binding flag parsing loop in cfg.zig
**File:** `src/cfg.zig:257-310`
**Severity:** LOW
**Status:** ✅ FIXED

`parseBindKey` and `parseUnbindKey` contained identical 20-line flag parsing loops for `-T` and `-n`.

### Root Cause
Duplicated code logic across key binding parsers.

### Fix
Extracted helper `parseKeyFlags` in `src/cfg.zig` to share flag parsing across `parseBindKey` and `parseUnbindKey`.
