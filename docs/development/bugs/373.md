---
type: bug
title: "Bug 373 — Benign EAGAIN on non-blocking display sockets disconnects the client"
description: "373. Display fds are O_NONBLOCK; a spurious POLLIN or transient WouldBlock makes handleClient return error.ReadFailed and run() tears down the client (reader freed, fd closed, display entry dropped). EAGAIN must be a no-op."
severity: "MEDIUM"
status: "✅ FIXED"
file: "src/server/server.zig:2231-2237 + caller 604-608"
timestamp: 2026-08-23T19:30:00Z
---

# Bug 373 — EAGAIN treated as fatal on display client reads

### 373. Transient WouldBlock tears down healthy connections
**File:** `src/server/server.zig:2231-2237`; caller `run()` at 604-608
**Severity:** MEDIUM
**Status:** ✅ FIXED

### Fix (applied)
`handleClient` now maps `error.WouldBlock` to a benign no-op return; only real
read failures propagate as `error.ReadFailed` and tear the client down.

**Test:** `handleClient tolerates EAGAIN on non-blocking fd — bug #373`
(non-blocking pipe with a registered MessageReader; handleClient must return
without error).

### Problem
```zig
const n = std.posix.read(fd, &buf) catch |err| {
    ...
    return error.ReadFailed;   // → removeClient(cfd)
```

`registerDisplayClient` flips the socket to non-blocking; on any spurious wake
or edge race the read returns `WouldBlock` and the whole DisplayClient is
destroyed instead of waiting for the next POLLIN.

### Fix (proposed)
Map `error.WouldBlock` to a benign zero-progress return in `handleClient`;
propagate only real errors as disconnects.
