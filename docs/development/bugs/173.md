---
type: bug
title: "Bug 173 — `c.kill` SIGWINCH return silently discarded — child may miss resize"
description: "173. c.kill SIGWINCH return silently discarded — child may miss resize"
severity: "MEDIUM"
status: "✅ FIXED — check kill return value, log warning on failure."
file: "`src/window.zig:108`"
sources:
  - src/window.zig
verified: human-reviewed
timestamp: 2026-08-03T00:00:00Z
---

# Bug 173 — `c.kill` SIGWINCH return silently discarded — child may miss resize

### 173. `c.kill` SIGWINCH return silently discarded — child may miss resize
**File:** `src/window.zig:108`
**Severity:** MEDIUM
**Status:** ✅ FIXED — check kill return value, log warning on failure.

```zig
_ = c.kill(pty.pid, c.SIG.WINCH);  // silently ignores failure
```
