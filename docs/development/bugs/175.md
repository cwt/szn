---
type: bug
title: "Bug 175 — Detach packet write return silently discarded — client may not receive detach"
description: "175. Detach packet write return silently discarded — client may not receive detach"
severity: "MEDIUM"
status: "✅ FIXED — replaced `_ = c.write(...)` with retry loop matching the partial-write pattern."
file: "`src/server/server.zig:531`"
sources:
  - src/server/server.zig
verified: human-reviewed
timestamp: 2026-08-03T00:00:00Z
---

# Bug 175 — Detach packet write return silently discarded — client may not receive detach

### 175. Detach packet write return silently discarded — client may not receive detach
**File:** `src/server/server.zig:531`
**Severity:** MEDIUM
**Status:** ✅ FIXED — replaced `_ = c.write(...)` with retry loop matching the partial-write pattern.

```zig
_ = c.write(cfd, ser.ptr, ser.len);  // silently ignores failure
```

---

## Updated Summary

| Severity | Count | Fixed | False Positive | Unresolved |
|----------|-------|-------|----------------|------------|
| Critical | 22 (18+4) | 19 | 3 | **0** |
| High | 41 (39+2) | 40 | 1 | **0** |
| Medium | 58 (52+6) | 56 | 2 | **0** |
| Low | 54 (54+0) | 51 | 3 | **0** |
| Total | 175 (163+12) | **166** | **9** | **0** |

---

## NEW BUGS (2026-06-27 audit of clock and tab completion changes)
