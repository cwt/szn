---
type: bug
title: "Bug 139 — `key_binding.zig` — Force unwrap in `mapCommandToAction` may panic"
description: "139. keybinding.zig — Force unwrap in mapCommandToAction may panic"
severity: "LOW"
status: "✅ FIXED — replaced `. ?` with `orelse return null`."
file: "`src/key_binding.zig:507`"
sources:
  - src/key_binding.zig
verified: human-reviewed
timestamp: 2026-08-03T00:00:00Z
---

# Bug 139 — `key_binding.zig` — Force unwrap in `mapCommandToAction` may panic

### 139. `key_binding.zig` — Force unwrap in `mapCommandToAction` may panic
**File:** `src/key_binding.zig:507`
**Severity:** LOW
**Status:** ✅ FIXED — replaced `. ?` with `orelse return null`.
