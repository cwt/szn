---
type: bug
title: "Bug 288 — `formatHelp` leaks `buf` on error"
description: "288. formatHelp leaks buf on error"
severity: "MEDIUM"
status: "✅ FIXED — added `errdefer buf.deinit(allocator)` to `formatHelp`. Regression test exercises the known-command, full-list, and unknown-command paths, freeing every returned slice."
file: "`src/cmd/cmd.zig:1513–1560`"
timestamp: 2026-08-03T00:00:00Z
---

# Bug 288 — `formatHelp` leaks `buf` on error

### 288. `formatHelp` leaks `buf` on error
**File:** `src/cmd/cmd.zig:1513–1560`
**Severity:** MEDIUM
**Status:** ✅ FIXED — added `errdefer buf.deinit(allocator)` to `formatHelp`. Regression test exercises the known-command, full-list, and unknown-command paths, freeing every returned slice.

```zig
var buf: std.ArrayList(u8) = .empty;
...
var line = try std.fmt.bufPrint(...);   // OOM → return error, buf never freed
```

No `errdefer buf.deinit(allocator)`. Any `try` failure leaks the accumulator. Called from `main.zig:116`.
