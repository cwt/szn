---
type: bug
title: "Bug 341 — `std.ArrayList(T).initCapacity()` is a pre-0.16 API removed in Zig 0.16"
description: "341. Three call sites replaced `std.ArrayList(T).initCapacity(allocator, N)` with `= .empty` + `try list.ensureTotalCapacity(allocator, N)` per Zig 0.16 migration rules."
severity: "HIGH"
status: "✅ FIXED"
file: "src/cfg.zig:367; src/grid.zig:909,914"
timestamp: 2026-08-09T00:00:00Z
---

# Bug 341 — `std.ArrayList(T).initCapacity()` is a pre-0.16 API removed in Zig 0.16

### 341. `initCapacity` — removed 0.16 API
**Files:** `src/cfg.zig:367`, `src/grid.zig:909,914`
**Severity:** HIGH
**Status:** ✅ FIXED

Zig 0.16 removed the managed `ArrayList.initCapacity(allocator, N)` constructor. The 0.16 pattern:

```zig
// FIXED:
var result: std.ArrayList(u8) = .empty;
try result.ensureTotalCapacity(allocator, s.len);
```

All three call sites updated. Tests and build verified clean.
