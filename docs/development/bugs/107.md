---
type: bug
title: "Bug 107 — `server/protocol.zig` — `IdentifyTerm.decode` missing `len <= 64` validation"
description: "107. server/protocol.zig — IdentifyTerm.decode missing len <= 64 validation"
severity: "HIGH"
status: "✅ FIXED — added `if (len > 64) return error.InvalidData` before the memcpy. Test added for len=65 rejection."
file: "`src/server/protocol.zig:89–97`"
timestamp: 2026-08-03T00:00:00Z
---

# Bug 107 — `server/protocol.zig` — `IdentifyTerm.decode` missing `len <= 64` validation

### 107. `server/protocol.zig` — `IdentifyTerm.decode` missing `len <= 64` validation
**File:** `src/server/protocol.zig:89–97`
**Severity:** HIGH
**Status:** ✅ FIXED — added `if (len > 64) return error.InvalidData` before the memcpy. Test added for len=65 rejection.
