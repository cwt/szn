---
type: bug
title: "Bug 140 — `key_binding.zig` — `val >= 0` is always true for `u8`"
description: "140. keybinding.zig — val = 0 is always true for u8"
severity: "LOW"
status: "✅ FIXED — removed dead `val >= 0` comparison."
file: "`src/key_binding.zig:509`"
sources:
  - src/key_binding.zig
verified: human-reviewed
timestamp: 2026-08-03T00:00:00Z
---

# Bug 140 — `key_binding.zig` — `val >= 0` is always true for `u8`

### 140. `key_binding.zig` — `val >= 0` is always true for `u8`
**File:** `src/key_binding.zig:509`
**Severity:** LOW
**Status:** ✅ FIXED — removed dead `val >= 0` comparison.
