---
type: bug
title: "Bug 317 — Sixel image refcounts leaked on grid history limit eviction"
description: "317. Grid.scrollUp deallocates oldest history line when exceeding history_limit without decrementing sixel refcounts."
severity: "HIGH"
status: "OPEN"
file: "src/grid.zig:234-236"
timestamp: 2026-08-09T00:00:00Z
---

# Bug 317 — Sixel image refcounts leaked on grid history limit eviction

### 317. Sixel image refcounts leaked on grid history limit eviction
**File:** `src/grid.zig:234-236`
**Severity:** HIGH
**Status:** OPEN

When `Grid.scrollUp` pushes lines into history beyond `history_limit`, it calls `deinit()` on the oldest history line.

Because `Grid` does not maintain ties to `Screen`'s sixel registry, sixel cell references on evicted history lines are never decremented.

### Root Cause
Direct deallocation of history lines in `Grid` bypasses Sixel registry reference counting.

### Impact
Continuous terminal scrolling leaks Sixel image references as lines fall off scrollback history.
