---
type: bug
title: "Bug 29 — Log file opened/closed on every log call"
description: "29. Log file opened/closed on every log call"
severity: "LOW"
status: "✅ FIXED"
file: "`src/main.zig:29–39`"
timestamp: 2026-08-03T00:00:00Z
---

# Bug 29 — Log file opened/closed on every log call

### 29. Log file opened/closed on every log call
**File:** `src/main.zig:29–39`
**Severity:** LOW
**Status:** ✅ FIXED

`logFn` does `fopen("/tmp/szn.log", "a")` and `fclose` on every single log call. Extremely slow under load. Should keep the file handle open or buffer writes.
