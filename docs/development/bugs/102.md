---
type: bug
title: "Bug 102 — `main.zig` — `errno` retrieval is always `.SUCCESS`, client disconnects on transient errors"
description: "102. main.zig — errno retrieval is always .SUCCESS, client disconnects on transient errors"
severity: "CRITICAL"
status: "✅ FIXED — replaced `std.posix.errno(-1)` with `std.c.errno(n)` which reads the actual errno via `_errno().*`. EAGAIN/EINTR now correctly prevent disconnect."
file: "`src/main.zig:56, 75, 149, 845, 873`"
sources:
  - src/main.zig
verified: human-reviewed
timestamp: 2026-08-09T00:00:00Z
---

# Bug 102 — `main.zig` — `errno` retrieval is always `.SUCCESS`, client disconnects on transient errors

### 102. `main.zig` — `errno` retrieval is always `.SUCCESS`, client disconnects on transient errors
**File:** `src/main.zig:56, 75, 149, 845, 873`
**Severity:** CRITICAL
**Status:** ✅ FIXED — replaced `std.posix.errno(-1)` with `std.c.errno(n)` which reads the actual errno via `_errno().*`. EAGAIN/EINTR now correctly prevent disconnect.
