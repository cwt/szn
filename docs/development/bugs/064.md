---
type: bug
title: "Bug 64 — Cursor position lost/reset on alternate screen exit (e.g. exiting Vim)"
description: "64. Cursor position lost/reset on alternate screen exit (e.g. exiting Vim)"
severity: "MEDIUM"
status: "✅ FIXED — Stored main cursor and main saved_cursor in `alt_cursor` / `alt_saved_cursor` fields when entering alternate screen, and restored them upon exit."
file: "`src/screen.zig:512`"
sources:
  - src/screen.zig
verified: human-reviewed
timestamp: 2026-08-03T00:00:00Z
---

# Bug 64 — Cursor position lost/reset on alternate screen exit (e.g. exiting Vim)

### 64. Cursor position lost/reset on alternate screen exit (e.g. exiting Vim)
**File:** `src/screen.zig:512`
**Severity:** MEDIUM
**Status:** ✅ FIXED — Stored main cursor and main saved_cursor in `alt_cursor` / `alt_saved_cursor` fields when entering alternate screen, and restored them upon exit.

---

## NEW BUGS (2026-06-24 full codebase audit)
