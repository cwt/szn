---
type: bug
title: "Bug 342 — `std.ArrayList.toOwnedSlice()` is removed in Zig 0.16"
description: "342. FALSE POSITIVE — `std.ArrayList.toOwnedSlice()` still exists in Zig 0.16 std lib. Both managed and unmanaged variants accept an allocator parameter and transfer ownership correctly."
severity: "HIGH"
status: "❌ FALSE POSITIVE"
file: "N/A — API still exists"
timestamp: 2026-08-09T00:00:00Z
---

# Bug 342 — `std.ArrayList.toOwnedSlice()` is removed in Zig 0.16

### 342. `toOwnedSlice` — FALSE POSITIVE
**Files:** N/A
**Severity:** HIGH (downgraded)
**Status:** ❌ FALSE POSITIVE

Initial analysis incorrectly flagged `std.ArrayList.toOwnedSlice()` as removed in Zig 0.16. Verification against the actual std lib source (`/opt/homebrew/Cellar/zig/0.16.0_1/lib/zig/std/array_list.zig`) confirms:

- **Managed variant** (line 101): `pub fn toOwnedSlice(self: *Self) Allocator.Error!Slice`
- **Unmanaged variant** (line 654): `pub fn toOwnedSlice(self: *Self, gpa: Allocator) Allocator.Error!Slice`

Both still exist and work correctly. The original code using `try list.toOwnedSlice(allocator)` is valid Zig 0.16.

**No code changes needed.**
