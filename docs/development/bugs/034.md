---
type: bug
title: "Bug 34 — split-window direction flag only works as first arg"
description: "34. split-window direction flag only works as first arg"
severity: "MEDIUM"
status: "✅ FIXED"
file: "`src/cmd/cmd.zig:112`"
sources:
  - src/cmd/cmd.zig
verified: human-reviewed
timestamp: 2026-08-03T00:00:00Z
---

# Bug 34 — split-window direction flag only works as first arg

### 34. split-window direction flag only works as first arg
**File:** `src/cmd/cmd.zig:112`
**Severity:** MEDIUM
**Status:** ✅ FIXED

`-v` / `-h` is checked only at `args[1]`. If the proportion comes first (e.g., `split-window 0.3 -v`), the flag is silently ignored.

---

## LOW (style, minor edge cases, future-proofing)
