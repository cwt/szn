---
type: bug
title: "Bug 100 — `client/raw.zig` — VMIN/VTIME indices are macOS values, completely wrong on Linux"
description: "100. client/raw.zig — VMIN/VTIME indices are macOS values, completely wrong on Linux"
severity: "CRITICAL"
status: "✅ FIXED — replaced hardcoded macOS values with `switch` on `@import(\"builtin\").os.tag`: Linux → VMIN=6/VTIME=5, macOS → VMIN=16/VTIME=17, FreeBSD → VMIN=4/VTIME=5. Test added to verify platform constants."
file: "`src/client/raw.zig:9–20`"
timestamp: 2026-08-03T00:00:00Z
---

# Bug 100 — `client/raw.zig` — VMIN/VTIME indices are macOS values, completely wrong on Linux

### 100. `client/raw.zig` — VMIN/VTIME indices are macOS values, completely wrong on Linux
**File:** `src/client/raw.zig:9–20`
**Severity:** CRITICAL
**Status:** ✅ FIXED — replaced hardcoded macOS values with `switch` on `@import("builtin").os.tag`: Linux → VMIN=6/VTIME=5, macOS → VMIN=16/VTIME=17, FreeBSD → VMIN=4/VTIME=5. Test added to verify platform constants.
