---
type: bug
title: "Bug 17 — Child uses parent allocator after fork"
description: "17. Child uses parent allocator after fork"
severity: "CRITICAL"
status: "✅ FIXED"
file: "`src/server/pty.zig:40–72`"
timestamp: 2026-08-03T00:00:00Z
---

# Bug 17 — Child uses parent allocator after fork

### 17. Child uses parent allocator after fork
**File:** `src/server/pty.zig:40–72`
**Severity:** CRITICAL
**Status:** ✅ FIXED

All C-string allocations moved before `fork()`. The child only reads the pre-populated argv array and never touches the parent's allocator.
