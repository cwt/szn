---
type: bug
title: "Bug 390 — Non-global `set prefix` updates the option display but not the dispatcher"
description: "390. cmdSetOption only refreshes server.dispatcher.prefix on the -g path; a session-scoped `set prefix C-a` changes the displayed option while C-b stays active. Config-file loading handles it correctly — the interactive command path doesn't."
severity: "MEDIUM"
status: "✅ FIXED"
file: "src/cmd/cmd.zig:889-901"
sources:
  - src/cmd/cmd.zig
verified: human-reviewed
timestamp: "2026-08-23T19:30:00Z"
---

# Bug 390 — Session-scoped `set prefix` silently ineffective

### 390. Display and behaviour diverge after non-global set
**File:** `src/cmd/cmd.zig:889-901`
(cf. correct config path server.zig:3080-3084)
**Severity:** MEDIUM
**Status:** ✅ FIXED

### Fix (applied)
`cmdSetOption`'s non-global session branch now updates
`server.dispatcher.prefix` when the option is `prefix` and the parsed value is
a key (mirroring both the `-g` path and the config-file loader).

**Test:** `session-scoped set prefix updates the dispatcher — bug #390`
(asserts both the stored option and live dispatcher state after
`set prefix C-a`).

### Problem
Only the `-g` branch updates `server.dispatcher.prefix`. The per-session path
stores into `session.options`, so `list-keys`/`show-options` report the new
prefix while key handling still uses the old one until restart or a global set.

### Fix (proposed)
Apply dispatcher.prefix whenever the affected session is the active session
(mirroring the config loader), or make prefix resolution read through the
session options at dispatch time.
