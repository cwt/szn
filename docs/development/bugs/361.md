---
type: bug
title: "Bug 361 — saved_grid churn leaks one full grid clone per copy/clock/choose toggle"
description: "361. Entering clock/copy/choose modes clones the grid into pane.saved_grid; leaving restores and 'frees' via the session arena (no-op). Every mode toggle permanently grows the session arena by a grid plus its cloned history."
severity: "HIGH"
status: "Fixed"
file: "src/window.zig:66-79, src/server/server.zig:1181, src/server/server.zig:1521, src/cmd/cmd.zig:622,639"
timestamp: 2026-08-31T05:20:00Z
---

# Bug 361 — saved_grid churn leaks a grid per mode toggle

### 361. Copy-mode / clock-mode / choose-buffer cycles grow memory forever
**File:** `src/window.zig:66-79`; clone sites `src/server/server.zig:1181`,
`src/server/server.zig:1521`, `src/cmd/cmd.zig:622,639`
**Severity:** HIGH
**Status:** Fixed

### Problem
`restoreSavedGrid` does `self.screen.grid.deinit()` before restoring — an arena
no-op. Overwrite-before-clone is correctly guarded everywhere (`if
(pane.saved_grid) |*g| { g.deinit(); ... }`), so ownership is never double-held;
the leak is pure arena bloat: one full grid (+ cloned history) retained forever
per toggle.

### Fix (proposed)
Same strategy as #359/#360: allocate saved grids from a resettable scratch
arena that is reset after restore, or stop cloning entirely by rendering
copy-mode from `(grid, scroll_offset)` state as tmux does.
