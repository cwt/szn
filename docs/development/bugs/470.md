---
type: bug
title: "Bug 470 — Copy-mode getCellAtY_i64 can overflow on extreme offsets"
description: "470. Negating INT64_MIN traps and scroll+extra uses wrapping arithmetic; inputs are small today but the path lacks saturation and an early empty return."
severity: "LOW"
status: "Fixed"
file: "src/mode_copy.zig:304-316"
sources: ["src/mode_copy.zig"]
verified: "machine-confirmed"
stale_after: "2026-12-31T00:00:00Z"
timestamp: "2026-09-12T00:00:00Z"
---

# Bug 470 — Copy-mode getCellAtY_i64 can overflow on extreme offsets

### 470. INT64_MIN negation + wrapping offset add
**Severity:** LOW
**Status:** Open
**Files:** [`src/mode_copy.zig`](../../../src/mode_copy.zig)

---

### Root Cause Analysis

In [`src/mode_copy.zig:304-307,313-316`](../../../src/mode_copy.zig#L304):

```zig
extra = @as(u32, @intCast(-y_i64)); // traps for INT64_MIN
return getCellAtOffset(..., scroll_offset + extra); // wrapping +
```

Inputs are `±history` today, but there is no `+|`/saturate or empty early-return.

### Proposed Fix

Saturate the negation/addition and return empty (null cell) on overflow instead of wrapping.
