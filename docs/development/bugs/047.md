---
type: bug
title: "Bug 47 — mapCommandToAction can match substrings"
description: "47. mapCommandToAction can match substrings"
severity: "MEDIUM"
status: "✅ FIXED"
file: "`src/key_binding.zig:433`"
timestamp: 2026-08-03T00:00:00Z
---

# Bug 47 — mapCommandToAction can match substrings

### 47. mapCommandToAction can match substrings
**File:** `src/key_binding.zig:433`
**Severity:** MEDIUM
**Status:** ✅ FIXED

`containsAtLeast(u8, trimmed, 1, "-h")` matches `-h` anywhere in the string. Flags like `-horizontal` or paths containing `-h` would incorrectly trigger.

---

## NEW BUGS (2026-06-22 codebase audit)
