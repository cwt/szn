---
type: bug
title: "Bug 25 — handleMouseFocus can use freed Pane pointer"
description: "25. handleMouseFocus can use freed Pane pointer"
severity: "HIGH"
status: "✅ FIXED"
file: "`src/server/server.zig`"
sources:
  - src/server/server.zig
verified: human-reviewed
timestamp: 2026-08-03T00:00:00Z
---

# Bug 25 — handleMouseFocus can use freed Pane pointer

### 25. handleMouseFocus can use freed Pane pointer
**File:** `src/server/server.zig`
**Severity:** HIGH
**Status:** ✅ FIXED

`handleMouseFocus` gets a `*Pane` from the layout tree, then calls `setActivePane` which may destroy the pane. Now validates pane is still alive after operations.
