---
type: bug
title: "Bug 66 — `setAttributes` fails to turn off removed attributes"
description: "66. setAttributes fails to turn off removed attributes"
severity: "CRITICAL"
status: "✅ FIXED — Changed `<` bitmask comparison to `(old & ~new) != 0`. Tests added for bold→italic (triggers reset) and bold→{bold,italic} (no reset)."
file: "`src/tty/tty.zig:154`"
sources:
  - src/tty/tty.zig
verified: human-reviewed
timestamp: 2026-08-03T00:00:00Z
---

# Bug 66 — `setAttributes` fails to turn off removed attributes

### 66. `setAttributes` fails to turn off removed attributes
**File:** `src/tty/tty.zig:154`
**Severity:** CRITICAL
**Status:** ✅ FIXED — Changed `<` bitmask comparison to `(old & ~new) != 0`. Tests added for bold→italic (triggers reset) and bold→{bold,italic} (no reset).
