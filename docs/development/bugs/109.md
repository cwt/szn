---
type: bug
title: "Bug 109 — `tty/fd_writer.zig` — Missing EINTR handling in writeAll and writeByte"
description: "109. tty/fdwriter.zig — Missing EINTR handling in writeAll and writeByte"
severity: "HIGH"
status: "✅ FIXED — both writeAll and writeByte now retry on EINTR. Pipe-based tests added verifying correct write-through."
file: "`src/tty/fd_writer.zig:16–17, 28–29`"
timestamp: 2026-08-03T00:00:00Z
---

# Bug 109 — `tty/fd_writer.zig` — Missing EINTR handling in writeAll and writeByte

### 109. `tty/fd_writer.zig` — Missing EINTR handling in writeAll and writeByte
**File:** `src/tty/fd_writer.zig:16–17, 28–29`
**Severity:** HIGH
**Status:** ✅ FIXED — both writeAll and writeByte now retry on EINTR. Pipe-based tests added verifying correct write-through.
