---
type: bug
title: "Bug 337 — Duplicated target window index resolution in cmd.zig"
description: "337. cmdMoveWindow and cmdSwapWindow duplicate target window index resolution logic."
severity: "LOW"
status: "✅ FIXED"
file: "src/cmd/cmd.zig:200-240"
timestamp: 2026-08-09T00:00:00Z
---

# Bug 337 — Duplicated target window index resolution in cmd.zig

### 337. Duplicated target window index resolution in cmd.zig
**File:** `src/cmd/cmd.zig:200-240`
**Severity:** LOW
**Status:** ✅ FIXED

`cmdMoveWindow` and `cmdSwapWindow` duplicated identical window index resolution logic.

### Root Cause
Duplicated window index resolution code.

### Fix
Extracted `resolveWindowIndices` helper in `src/cmd/cmd.zig` and updated both commands to use it.

### Impact
Boilerplate duplication across window management commands.
