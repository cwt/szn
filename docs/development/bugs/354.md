---
type: bug
title: "Bug 354 — Remote panic: @intCast(idx) u32→u8 on SGR indexed-colour params (6 sites)"
description: "354. screen.zig setSgr narrows saturated-u32 CSI params to u8 via @intCast at six sites. `\\x1b[38;5;999m`, `\\x1b[48;2;300;0;0m` etc. from any child process panic the server."
severity: "CRITICAL"
status: "🔴 OPEN"
file: "src/screen.zig:1306,1316,1332,1342,1358,1362"
timestamp: 2026-08-23T19:30:00Z
---

# Bug 354 — Remote panic: SGR colour params @intCast truncation

### 354. `\x1b[38;5;999m` kills the whole server
**File:** `src/screen.zig:1306,1316,1332,1342,1358,1362` (reached from
`src/input.zig:743-759`)
**Severity:** CRITICAL
**Status:** 🔴 OPEN

### Problem
Any pane output containing an out-of-range SGR colour parameter crashes szn in
Debug/ReleaseSafe ("integer does not fit in destination type" — empirically
confirmed with Zig 0.16.0). ReleaseFast silently wraps to a wrong palette slot.

### Root Cause
The CSI param parser deliberately saturates values to `maxInt(u32)` (bug #99,
input.zig:274) — but `setSgr` then does:

```zig
const idx = params[i];
i += 1;
self.cur_cell.fg = Colour.fromIndexed(@intCast(idx));   // idx: u32 → u8
```

at all six indexed-colour sites (38;5 / 48;5 foreground+background pairs).
The saturating discipline stops exactly where the cast happens.

### Fix (proposed)
Clamp/saturate before conversion, e.g.
`Colour.fromIndexed(@intCast(@min(idx, 255)))` or truncate with a documented
policy. Regression tests: feed `\x1b[38;5;999m`, `\x1b[48;2;300;0;0m`,
`\x1b[38;2;999;999;999m` — expect defined colours, no panic.
