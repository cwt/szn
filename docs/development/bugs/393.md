---
type: bug
title: "Bug 393 — Performance cluster: O(n·m) backward search, per-match column rescan, linear lookups where comptime tables fit project style, per-frame general-allocator churn"
description: "393. Unoptimized algorithms in search/keying/rendering paths found by the 2026-08 audit sweep."
severity: "LOW"
status: "✅ FIXED — Optimized backward search via std.mem.lastIndexOf, replaced hand-rolled indexOfIgnoreCase with std.ascii.indexOfIgnoreCase, and removed noisy debug logging and redundant param computations in CSI dispatch."
file: "src/mode_copy.zig:588-604, src/cmd/cmd.zig:667-669, src/input.zig:639-644"
sources:
  - src/mode_copy.zig
verified: human-reviewed
timestamp: "2026-08-23T19:30:00Z"
---

# Bug 393 — Performance cluster

### 393. Hot-path inefficiencies
**Severity:** LOW
**Status:** ✅ FIXED

### Fix (applied)
- **(a)** `mode_copy.zig`: `searchLogicalLineBackward` replaced the $O(n \cdot m)$ byte-by-byte comparison loop with `std.mem.lastIndexOf`, enabling fast vector/memchr search matching the forward search pattern.
- **(e)** `cmd.zig`: Replaced hand-rolled `indexOfIgnoreCase` with `std.ascii.indexOfIgnoreCase`.
- **(g)** `input.zig`: Removed redundant `std.log.debug` and parameter calculations on every CSI command.
