---
type: bug
title: "Bug 393 — Performance cluster: O(n·m) backward search, per-match column rescan, linear lookups where comptime tables fit project style, per-frame general-allocator churn"
description: "393. Unoptimized algorithms in search/keying/rendering paths found by the 2026-08 audit sweep."
severity: "LOW"
status: "🔴 OPEN"
file: "src/mode_copy.zig:603-610,673, src/key_binding.zig:65-89, src/options.zig:144-149, src/cmd/cmd.zig:622-638, src/server/server.zig:2832,2878, src/status.zig:62-247, src/input.zig:613"
timestamp: "2026-08-23T19:30:00Z"
---

# Bug 393 — Performance cluster

### 393. Hot-path inefficiencies
**Severity:** LOW
**Status:** 🔴 OPEN

| # | Site | Finding |
|---|------|---------|
| a | mode_copy.zig:603-610 | `searchLogicalLineBackward` decrements byte_pos by 1 running mem.eql at every offset — O(n·m). Forward search uses memchr-accelerated indexOf (:577). Use `lastIndexOf`. |
| b | mode_copy.zig:673 | `byteToColumn` restarts a linear scan from column 0 for every match. Track cumulative offsets during the scan instead. |
| c | key_binding.zig:65-89 | `KeyTable.bind/unbind/lookup` linear scan per keystroke. AGENTS.md prefers comptime-generated binding tables; StaticStringMap fits defaults. |
| d | options.zig:144-149 | `Options.findDef` linear scan per access over a static table → `std.StaticStringMap`. |
| e | cmd.zig:622-638 | Hand-rolled O(n·m) `indexOfIgnoreCase`. |
| f | server.zig:2832 | `format_mod.Context.init(allocator)` builds a StringHashMap **per pane border per frame** inside renderToDisplayClient; status rebuild allocates/frees a dozen intermediates per dirty frame (status.zig:62-247) plus an extra dupe at server.zig:2878. Violates the arena-per-frame convention — route through a reset scratch arena. |
| g | input.zig:613 | `std.log.debug` fires for every CSI of every pane in Debug builds; also recomputes `self.param(0)` next to an existing value. Gate behind a flag. |
